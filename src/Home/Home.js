import React from 'react';
import SearchFood from '../SearchFood/SearchFood';

const Home = () => {
    return (
        <div>
            <SearchFood></SearchFood>
        </div>
    );
};

export default Home;